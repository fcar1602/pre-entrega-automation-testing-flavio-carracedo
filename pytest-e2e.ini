[pytest]
addopts = -q --strict-markers --html=report.html --self-contained-html
testpaths = entregaFinal/tests
python_files = test_*.py
filterwarnings = ignore::DeprecationWarning
markers =
    smoke: smoke tests
    exception: tests that verify exceptions
